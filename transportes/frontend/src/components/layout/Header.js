import React from "react";

const Header = (props) => {
    return (
        <header>
            <img src="images/logo.png" width="100" alt="Transportes X" />
            <h1>Transportes X</h1>
        </header>
    );
}

export default Header;